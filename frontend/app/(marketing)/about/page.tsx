import StubPage from "@/components/landing/StubPage";

export const metadata = { title: "About — SkillBridge" };

export default function AboutPage() {
  return (
    <StubPage
      title="About"
      description="We're building the tool we wished existed when we were applying to our first jobs out of school."
    />
  );
}
