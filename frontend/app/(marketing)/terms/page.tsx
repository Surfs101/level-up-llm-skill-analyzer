import StubPage from "@/components/landing/StubPage";

export const metadata = { title: "Terms of service — SkillBridge" };

export default function TermsPage() {
  return (
    <StubPage
      title="Terms of service"
      description="Our terms of service are being finalized."
    />
  );
}
