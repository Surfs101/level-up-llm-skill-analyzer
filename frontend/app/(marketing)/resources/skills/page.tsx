import StubPage from "@/components/landing/StubPage";

export const metadata = { title: "Skills taxonomy — SkillBridge" };

export default function SkillsTaxonomyPage() {
  return (
    <StubPage
      title="Skills taxonomy"
      description="A browseable view of every skill we recognize is coming soon. For now, run an analysis to see your matched and missing skills."
    />
  );
}
