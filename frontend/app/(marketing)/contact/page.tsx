import StubPage from "@/components/landing/StubPage";

export const metadata = { title: "Contact — SkillBridge" };

export default function ContactPage() {
  return (
    <StubPage
      title="Contact"
      description="Email contact@skillbridge.app. We read everything."
    />
  );
}
