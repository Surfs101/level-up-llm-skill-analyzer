import StubPage from "@/components/landing/StubPage";

export const metadata = { title: "Pricing — SkillBridge" };

export default function PricingPage() {
  return (
    <StubPage
      title="Pricing"
      description="We're free to use while in beta. We'll publish pricing here when we launch paid plans."
    />
  );
}
